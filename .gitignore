# Release binaries are uploaded to GitHub Releases, not committed.
*.deb
*.deb.sha256

# Research downloads (rosbags etc. are huge)
research/downloads/

# Run outputs (TUM trajectories, maps, logs)
output/
research/logs/

# Machine guide contains personal info; keep out of public repos by default
MACHINE_GUIDE.md
